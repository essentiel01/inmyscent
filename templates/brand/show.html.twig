{% extends 'base.html.twig' %}

{% block title %}Brand{% endblock %}

{% block main %}
<div class="container brand-container">
    <div class="btn-create">
        <h1>La marque</h1>
        <a class="btn btn-warning" href="{{ path('brand_edit', {'id': brand.id}) }}">Modifier</a>

        <a class="btn btn-secondary" href="{{ path('brand_index') }}">Retour à la liste</a>
    </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brand.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ brand.name }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('brand/_delete_form.html.twig') }}
</div>
{% endblock %}
